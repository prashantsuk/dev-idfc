micronaut:
  server:
    port: 8099
  application:
    name: msDashBoardMetrics
   
netty:
  default:
    allocator:
      max-order: 3

aerospike:
  namespace: test
  host: localhost
  port: 3000
  
eureka:
  client:
    registration:
      enabled: true
    defaultZone: "${EUREKA_HOST:localhost}:${EUREKA_PORT:8761}"


    

